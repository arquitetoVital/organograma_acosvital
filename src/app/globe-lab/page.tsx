import GlobeLab from './GlobeLab';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Globe Lab — Aços Vital',
};

export default function GlobeLabPage() {
  return <GlobeLab />;
}
